<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Guild Helper</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .error-container {
      background: white;
      padding: 50px;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      text-align: center;
      max-width: 600px;
      width: 100%;
    }

    .error-icon {
      font-size: 64px;
      margin-bottom: 20px;
    }

    h1 {
      color: #2d3748;
      font-size: 24px;
      margin-bottom: 15px;
    }

    p {
      color: #718096;
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 30px;
    }

    .btn {
      display: inline-block;
      padding: 12px 30px;
      background: #667eea;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      transition: all 0.2s;
      margin: 5px;
    }

    .btn:hover {
      background: #5a67d8;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }

    .guilds-section {
      margin-top: 30px;
      text-align: left;
    }

    .guilds-section h2 {
      color: #2d3748;
      font-size: 20px;
      margin-bottom: 20px;
      text-align: center;
    }

    .guild-list {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .guild-item {
      display: flex;
      align-items: center;
      padding: 15px;
      background: #f7fafc;
      border-radius: 12px;
      border: 2px solid #e2e8f0;
      transition: all 0.2s;
    }

    .guild-item:hover {
      border-color: #667eea;
      box-shadow: 0 2px 8px rgba(102, 126, 234, 0.2);
    }

    .guild-icon {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      margin-right: 15px;
      background: #cbd5e0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
    }

    .guild-icon img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
    }

    .guild-info {
      flex: 1;
    }

    .guild-name {
      font-weight: 600;
      color: #2d3748;
      font-size: 16px;
      margin-bottom: 4px;
    }

    .guild-members {
      color: #718096;
      font-size: 14px;
    }

    .join-note {
      margin-top: 20px;
      padding: 15px;
      background: #ebf8ff;
      border-left: 4px solid #4299e1;
      color: #2c5282;
      font-size: 14px;
      text-align: left;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-icon"><% if (typeof showGuildLinks !== 'undefined' && showGuildLinks) { %>🚪<% } else { %>❌<% } %></div>
    <h1><% if (typeof showGuildLinks !== 'undefined' && showGuildLinks) { %>Guild Membership Required<% } else { %>Oops! Something went wrong<% } %></h1>
    <p><%= message %></p>
    
    <% if (typeof showGuildLinks !== 'undefined' && showGuildLinks && typeof botGuilds !== 'undefined') { %>
      <div class="guilds-section">
        <h2>Join one of these servers:</h2>
        <div class="guild-list">
          <% botGuilds.forEach(guild => { %>
            <div class="guild-item">
              <div class="guild-icon">
                <% if (guild.icon) { %>
                  <img src="<%= guild.icon %>" alt="<%= guild.name %>">
                <% } else { %>
                  🛡️
                <% } %>
              </div>
              <div class="guild-info">
                <div class="guild-name"><%= guild.name %></div>
                <div class="guild-members"><%= guild.memberCount %> members</div>
              </div>
            </div>
          <% }); %>
        </div>
        <div class="join-note">
          <strong>Note:</strong> Contact a server administrator for an invite link to join one of these guilds and access the Guild Helper bot features.
        </div>
      </div>
    <% } %>
  </div>
</body>
</html>
